<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Hosting and IPC-security tests. Separate from BladeControl.Runtime.Tests because the
    service host and the pipe ACL are Windows-only (net8.0-windows), whereas the runtime core
    and its tests are deliberately platform-neutral.

    Nothing here opens hardware, registers a service, or installs anything.
  -->
  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.8.0" />
    <PackageReference Include="MSTest.TestAdapter" Version="3.1.1" />
    <PackageReference Include="MSTest.TestFramework" Version="3.1.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\BladeControl.Ipc\BladeControl.Ipc.csproj" />
    <ProjectReference Include="..\..\src\BladeControl.Service\BladeControl.Service.csproj" />
  </ItemGroup>

  <!--
    Shared, not copied. Driving the real pipe server end to end needs a real BladeRuntime behind
    it, which means fakes for eleven interface members whose return types are awkward to build.
    Those fakes already exist next door; a second copy would be a second thing to keep true.
  -->
  <ItemGroup>
    <Compile Include="..\BladeControl.Runtime.Tests\RuntimeTestDoubles.cs"
             Link="Shared\RuntimeTestDoubles.cs" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Microsoft.VisualStudio.TestTools.UnitTesting" />
  </ItemGroup>

</Project>
